# 本地开发：headless=false 会尝试自动打开浏览器（可用环境变量覆盖）
[server]
headless = false
address = "127.0.0.1"
port = 8501
# Windows 下 watchdog 偶发导致进程异常退出，改为 none 更稳
fileWatcherType = "none"
runOnSave = false

[browser]
gatherUsageStats = false
