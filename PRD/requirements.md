# 需求记录

> 来源：多轮需求讨论的提取汇总，每轮对话后追加。正式 PRD 冻结后，对应条目标记为"已入PRD"。

---

## 第 0 轮（项目初始化，2026-05-16）

- **R001**：项目命名为 ESS_simulink，位于 `E:\Cursor\ESS_simulink`
- **R002**：构建储能运营模拟计算器，计算不同电价模式、不同主体、不同商业模式下的综合收益
- **R003**：电价模式维度：电力市场电价、行政分时电价等
- **R004**：主体维度：售电公司、储能投资商、终端用户等
- **R005**：商业模式维度：不同合作/分成/盈利模式
- **R006**：核心目的：帮助用户理解所有外部条件如何传导到储能收益
- **R007**：支持用户通过下拉选择不同地区（如 henan、未来可能的 guangdong）的数据做分析
- **R008**：项目未来包含复杂的 Python 优化算法程序
- **R009**：虚拟环境用 Python 3.11+，核心依赖 pandas/numpy/scipy/cvxpy/FastAPI/uvicorn；前端采用纯 HTML/CSS/JS + ECharts，由 FastAPI 静态托管

## 第 1 轮（数据处理，2026-05-16）

- **R010**：负荷原始数据（分钟级，含 load/storage/soc 列）→ 计算 Load_real = load - storage，聚合到小时级（算术平均），按地区合并，缺失值智能修补
- **R011**：现货电价原始数据（分钟级，每小时内值相同）→ 去重到小时级，保留 day_ahead 和 real_time
- **R012**：全局单位注册表，集中管理所有数据字段的单位（元/kWh、元/MWh、kWh 等）
- **R013**：数据文件按 `<类型>_<地区>.csv` 命名，source 列标识来源
- **R014**：数据处理脚本支持命令行参数传入地区名，可复用于未来新增地区

---

## 第 2 轮（需求讨论，2026-05-16）

- **R015**：项目功能模式——用户通过滑块、自定义参数、数据选择等方式配置场景，支持单方案分析和多方案对比，工具智能分析方案差异原因
- **R016**：三个业务角色——售电公司、储能投资商（=运营商）、终端用户
- **R017**：物理计量关系——load_grid = load_real - load_ESS，load_real 是终端用户唯一结算依据
- **R018**：储能充放电符号约定——充电为正、放电为负（待确认与导入公式的一致性）
- **R019**：电价模式1——行政分时电价（无售电）：电网公司设定的峰谷平电价，用户直接向电网缴费
- **R020**：电价模式2——行政分时电价（带售电，江苏模式，标记"即将淘汰"）：售电公司给月度不分时平段电价 P_flat，峰=1.6×P_flat，谷=0.4×P_flat，平=P_flat；用户另向电网缴纳输配电价等
- **R021**：电价模式3——合同分时电价：售电公司与用户约定固定峰谷平电价
- **R022**：电价模式4——现货联动电价：每小时电价不同，但同一天不同小时对应 = 售电公司当月日前现货算术平均电价
- **R023**：电价模式5——售电公司一口价：24小时统一固定电价
- **R024**：商业模式1——仅用户+储能投资商（无售电），行政分时电价，用户向电网交分时电费 + 向运营商支付"返还费用"
- **R025**：商业模式2——三方独立，分别优化各自收益（2a:售电最优/2b:储能最优/2c:用户最优）
- **R026**：商业模式3——储售一体 vs 用户独立（3a:储售一体收益最高/3b:用户收益最高）
- **R027**：商业模式4——三方一体，总体收益最高
- **R028**：关键公式——储能收益 = Σ(储能各小时充放电量 × 各小时电价)
- **R029**：关键公式——用户返还给运营商的费用 = 储能收益 × (1 - 用户侧分成比例)
- **R030**：需人工录入——售电合同模式及参数、中长期合约（量/价）、日前报电量数据、储能参数
- **R031**：算法核心——根据商业模式计算储能最优运行策略（每小时充放电量）
- **R032**：售电公司购电成本 = 中长期电费 + 日前电费 + 实时电费
- **R033**：本次不考虑需量电费和光伏
- **R034**：假设售电公司只有一个用户（终端用户），用户用电量 = 售电公司实际购电量
- **R035**：SOC 约束——0%~100%，充放电不能同时进行，跨日连续传递
- **R036**：储能往返效率约 85%，单程效率 = sqrt(往返效率)
- **R037**：储能功率约束——最大充放电功率通常 = 额定容量 × 0.5C
- **R038**：等效满充满放次数 = 总放电量 / 额定容量
- **R039**：投资收益指标——IRR、NPV、静态投资回收期
- **R040**：建设单价 700~1200 元/kWh，磷酸铁锂设计寿命 10 年
- **R041**：敏感性分析变量——建设单价、用户分成比例、合同价差、日前价差、调度策略、运维比例、项目寿命
- **R042**：容量衰减——第N年有效容量 = 初始容量 × (1 - 年衰减率)^N，年衰减率通常 2%~3%
- **R043**：月度批发合同（中长期+日前+实时），售电公司从中长期和现货市场组合购电
- **R044**：江苏模式中，用户额外向电网缴纳输配电价、线损费用、系统运行费、政府性基金及附加（按用电量×核定单价）
- **R045**："运营商"、"储能投资商"等描述统一指代储能投资商（ESS投资者/运营商）

---

## 第 3 轮（需求澄清，2026-05-16）

- **R046**：储能符号约定确认——充电为负、放电为正，storage = load_ESS
- **R047**：CSV 原始列含义确认——load = load_grid（关口表读数），storage = load_ESS（储能功率）
- **R048**：Load_real = load + storage（需修正已处理数据！此前用 load - storage 是错的）
- **R049**：Q2确认——现货联动电价 = 各小时全月 day_ahead 算数平均，每天同小时价格相同
- **R050**：用户电费节省 = 无储能时的电费 - 有储能时的电费；用户支付给运营商的费用 = 电费节省 × (1 - 用户分成比例)
- **R051**：售电公司购电量 = load_grid（关口表读数），而非 load_real
- **R052**：商业模式3"储售一体"= 售电公司和储能投资商是同一主体，无需内部转移
- **R053**："XX收益最高"指以该目标优化储能调度曲线，不同优化目标导致不同的充放电决策
- **R054**："用户保留储能收益 × 用户分成比例"表述不准确，实际是用户电费账单减少额中，运营商拿走 (1-分成比例)，用户净留存 分成比例
- **R055**：合同电价和现货购电成本可能存在价差矛盾（如0:00合同100但现货200），不同优化目标会产生相反的调度决策

## 第 4 轮（数据口径 & Mock 数据，2026-05-16）

- **R056**：全部计算为小时级，1kW 数值 = 1kWh，所有功率数据直接视为电量数据（kWh）
- **R057**：储能默认参数（η、衰减率等）需在 Web UI 中提供修改入口
- **R058**：当前仅以河南为例，先创建 mock 数据，后期提醒替换为真实数据
- **R059**："江苏模式"仅为代号，在河南的全部数据中也应包含该模式
- **R060**：一期不考虑输配电价、线损、系统运行费、政府基金、需量电费——用户电费 = 纯电能量电费
- **R061**：中长期/现货/实时购电成本计算逻辑将在后续专题中详细讨论
- **R062**："即将淘汰"标记仅作提示，功能仍需实现
- **R063**：储能调度输出 = 每小时一个充放电电量值（kWh），一天 24 个点
- **R064**：P_flat → 重命名为"月度平段基础电价"（江苏模式中售电公司给用户的不分时基础价）
- **R065**：Mock 数据文件放在 data/config/ 下，包括行政分时电价、江苏模式、合同分时电价模板、储能默认参数
- **R066**：合同分时电价的时段结构（峰/平/谷）与行政分时电价相同，但具体时段划分和价格均独立设定
- **R067**：所有参数均可灵活调整，用户可保存为方案、创建多方案并对比各维度数据

## 第 5 轮（售电公司购电成本，2026-05-16）

- **R068**：售电公司购电成本 C = C_中长期 + C_日前 + C_实时
- **R069**：C_中长期 = Σ(Q_合约,t × P_合约,t)，中长期合约包括年度双边、月度集中竞价等
- **R070**：C_日前 = Σ[(Q_日前,t - Q_合约,t) × P_日前,t]，量差结算——日前出清电量与合约电量的偏差按日前价格结算
- **R071**：C_实时 = Σ[(Q_实际,t - Q_日前,t) × P_实时,t]，Q_实际,t = load_grid[t]
- **R072**：购电成本中仅 Q_实际,t（=load_grid）受储能运行影响，Q_合约和 Q_日前为预定量
- **R073**：储能对购电成本的影响仅通过实时偏差电费传导：ΔC = Σ[(-load_ESS[t]) × P_实时,t]
- **R074**：需 mock 数据：Q_合约,t、P_合约,t、Q_日前,t（三组小时级时间序列）
- **R075**：后端严格按真实市场规则计算，不做简化或近似——即使当前售电公司只有一个用户、用户电量与购电电量 1:1 联动，后端也独立计算各环节
- **R076**：架构需支持未来扩展（如一家售电公司下挂多个用户），前端可做展示层合并，后端不能偷工减料

## 第 6 轮（PRD 修正 & 参数管理体系，2026-05-16）

- **R077**：运营商年运维成本 = 储能初始投资 × 1%
- **R078**：储能初始投资单价 = 0.9 元/Wh（= 900 元/kWh）
- **R079**：所有涉及参数的项目都应放在 Web 初始参数设置页面中
- **R080**：全局初始参数页面包含全部可调节参数，作为系统参数库
- **R081**：方案 = 全局默认参数 + 方案私有参数覆盖；新建方案自动加载全局默认参数
- **R082**：用户可从全局参数库选择性添加参数到方案中作为私有参数，未添加的沿用全局默认值
- **R083**：新建对比方案（方案二）默认加载全局缺省参数，不继承方案一的私有参数
- **R084**：任意方案可一键复制其他已保存方案的全部或部分私有参数

## 第 7 轮（售电购电数据加载与验收，2026-05-22）

- **R085**：`contract_position_*.csv`、`dayahead_position_*.csv` 在含 `date` 列时，计算路径必须按 `ScenarioConfig.selected_date` 筛选该日 0~23 时共 24 行，再代入 §5.3 购电公式，与现货/负荷按日对齐。**已入PRD**
- **R086**：筛选后该日无行或小时不完整时，应抛出明确错误（提示检查 CSV 日期与行数）。**已入PRD**
- **R087**：购电分项验收：`C_中长期`、`C_日前`、`C_实时` 与 PRD 手工展开式在固定 mock 数据上一致；`C_购电 = 三者之和`。**已入PRD**

## 第 8 轮（前端架构拆分 + 后端架构准备，2026-05-31）

- **R088**：前端从单文件 index.html（1299行）拆分为独立 JS 模块：`app.js`（全局状态/API/导航）、`charts.js`（ECharts图表）、`flow.js`（能量流动图SVG）、`analysis.js`（单方案分析）、`params.js`（缺省参数）、`compare.js`（多方案对比占位），CSS抽取到 `style.css`，通过 `window.App` 命名空间通信，HTML内联onclick通过全局代理转发
- **R089**：文件名带 `.v4` 后缀用于浏览器缓存破坏，HTML引用 `.v4` 版本
- **R090**：后端新增算法注册表（`src/core/registry.py`），使用 `@register_algorithm` 装饰器注册调度算法，现有 `optimize_arbitrage` 作为默认算法注册，未来新增算法只需新增文件+装饰器
- **R091**：后端 API 结果缓存（`routes.py` 中 `_result_cache` 字典，100条LRU），key 为 `scenario_id|pricing_mode|business_model|settlement|contract|dayahead`
- **R092**：ESS参数重构——`ESSParams` 新增字段：`power_rated`（额定功率MW）、`eta_charge`（单程充电效率）、`degrade_enabled`（年衰减开关）、`cycle_life`（循环次数）、`cycle_enabled`（循环约束开关）；`cap_rated` 默认值从5000改为1000kWh
- **R093**：新增光伏参数体系——`pv_defaults.csv`（容量、单价、运维比例、设计寿命、衰减率、地区、曲线类型）、`pv_curves.csv`（3地区×3曲线类型，24小时归一化出力数据）、前端光伏参数面板（含曲线选择和ECharts预览）
- **R094**：财务参数调整——移除 `r_user_b3`，保留 `r_discount`、`r_user_b1`、`r_user_b2`；B3模型 fallback 到 `r_user_b2`
- **R095**：并行开发规则——两个Claude窗口可并行工作，Session 1 负责 analysis/charts/flow.js，Session 2 负责 params.js，共享文件（app.js/style.css/index.html/routes.py）禁止同时编辑，编辑前需确认对方未在编辑
- **R096**：缺省参数页面click无响应问题——根因是 JS 拆分后 `toggleSection` 和 `switchTariffTab` 函数缺失导致 params.js 整体加载失败，`App.params` 从未被赋值；修复方法为补全缺失函数定义
- **R097**：禁止自动 git push——未经用户明确指示，不执行 git push

## 第 9 轮（中长期量价曲线 + 参数面板完善，2026-05-31）

- **R098**：中长期量价曲线面板——缺省参数页面新增"中长期量价曲线"面板，包含：合约电量文本框（MWh）/覆盖比例（%）单选切换、合约价格输入框、分解曲线下拉框（统调负荷/D1~D5）、24小时柱状图（按分时电价着色）、说明文本
- **R099**：合约电量分解算法——`GET /api/params/contract-curve` 端点，支持6种分解方式：load（统调负荷加权）、D1（全时段价格加权）、D2（平均分配）、D3（仅峰段）、D4（仅平段）、D5（仅谷段）
- **R100**：统调负荷 mock 数据——`data/config/system_load_henan.csv`，河南地区典型日24小时统调负荷（MW），用于合约分解计算
- **R101**：分时电价时段定义硬编码在 routes.py 中（valley 0-8, peak 8-12/17-21, flat 12-17/21-24），后续应改为从 CSV 读取

## 第 10 轮（方案概览区重构 + 经济性评价，2026-05-31）

- **R102**：方案概览区重构——移除原有7个KV标签（现货峰谷价差、分时峰谷价差、时段构成、净负荷特征等），保留运营状态标签并移至图片下方
- **R103**：运营状态标签（区域3）——两行×三列：行1为储能日均充放次数、单次最大放电深度、单次最小放电深度；行2为光伏本地消纳率、光伏年利用小时数、用户负荷变异系数（后三项暂为占位值）
- **R104**：经济性评价系统（区域2.2）——六级评级：极差/较差/差/达标/优秀/无敌，评价规则：
  - 光伏/储能投资：IRR% — (≤0, (0,2], (2,4], (4,8], (8,12], >12)
  - 售电公司：度电收益(元/MWh) — (<0, [0,1), [1,3), [3,5), [5,8), ≥8)
  - 终端用户：储能节费(万元/MWh) — (<0, [0,2), [2,4), [4,7), [7,10), ≥10)
- **R105**：经济性评价卡片UI——采用系统一级模块卡片样式（`var(--bg-card)` + `var(--border)` + `var(--radius)` + `18px 20px`内边距），标题用 `.card-hd` + `.card-title`（左侧3px蓝色竖线），内部三列网格（指标说明/核心数据/pill标签），标签色彩：优秀用主色蓝、较差用告警橙、无数据用中性灰
- **R106**：API新增字段——`CalculateResponse` 新增 `econ_ratings`（经济性评级列表）、`load_cv`（负荷变异系数）；`InvestmentData` 新增 `retail_profit_wan`（售电公司利润万元）；`OverviewData` 新增 `pv_cap_kw`、`flex_load_kw`
- **R107**：方案槽位系统——参数调节区底部新增5个槽位（A~E）+ `+`按钮 + `比较`按钮。逻辑：点击`+`记录当前参数快照到最左空槽位并亮起字母，`+`随即disable；调整参数后`+`保持disable（联动"参数已修改"标签）；点击"计算"后`+`恢复enable（联动"计算完成"标签）；点击已激活槽位则取消记录；`比较`按钮弹出空弹窗（待实现）
- **R108**：`+`按钮状态联动——与标题栏"参数已修改"/"计算完成"标签同步：参数修改→disable，计算完成→enable，无空槽位→disable

---

## 第 11 轮（光伏计算全链路贯通，2026-06-06）

- **R109**：新增 `PVParams` 数据模型——`src/models/dispatch.py` 新增 `PVParams` dataclass，字段与 `pv_defaults.csv` 一致（cap_rated、feed_in_tariff、self_use_discount、unit_cost、r_om、design_life、r_degrade_first、r_degrade），含 `initial_investment` 属性
- **R110**：`DispatchResult` 新增光伏字段——`pv_generation`、`pv_self_consumed`、`pv_fed_in`（24 小时列表）、`pv_cap_kw`、`pv_total_gen_daily`、`pv_self_daily`、`pv_feed_in_daily`、`pv_self_rate`、`pv_irr`、`pv_npv`、`pv_payback_years`
- **R111**：光伏调度计算逻辑——`dispatch.py` 的 `run_dispatch()` 新增 `pv_params` 和 `pv_curve` 可选参数；计算每小时 PV 发电量 = cap_rated × curve[h]；PV 自发自用 = min(pv_gen, max(0, load_real - load_ESS))；修正 load_grid = load_real - load_ESS - pv_self；PV 投资指标含首年衰减差异
- **R112**：`calculator.py` 集成光伏——加载 `pv_defaults.csv` 和 `pv_curves.csv`，构建 `PVParams` 传入 `run_dispatch`
- **R113**：API 响应扩展——`TimeSeries` 新增 `pv_power` 字段；新增 `PVInvestmentData` schema（initial_invest_wan、irr_pct、payback_years、cum_cf_wan、daily_gen_kwh、annual_gen_mwh、daily_self_yuan、annual_self_wan、daily_feed_in_yuan、annual_feed_in_wan、self_rate）；`CalculateResponse` 新增 `pv_investment` 可选字段；`OverviewData.pv_cap_kw` 从计算结果填充
- **R114**：光伏经济性评级——`_compute_econ_ratings()` 中光伏 IRR 从占位 `None` 替换为实际计算值
- **R115**：前端光伏投资数据渲染——`analysis.v4.js` 的 `renderResult()` 中 12 个 `'--'` 占位替换为 `r.pv_investment` 实际数据；运营状态标签（光伏自用率、利用小时数）同步更新；多方收益区光伏上网/消纳收益填充

---

## 第 12 轮（底层数据重构与可信度登记，2026-06-07）

- **R116**：明确数据维度口径：用户典型负荷和中长期/日前持仓按 profile/交易策略管理，不按 region 管理；行政分时电价、现货价格、光伏出力、统调负荷、需量/容量单价按 region 管理。
- **R117**：新增持久化数据策略文档与 TODO：`docs/DATA_STRATEGY_NOTES.md`、`docs/DATA_REFACTOR_TODO.md`，用于多电脑开发时同步当前数据口径和后续待办。
- **R118**：新增轻量可信度登记文件 `data/catalog/data_trust.csv`，只记录可信等级（mock/web_collected/official_api/manual_verified 等）、是否 mock、适用范围和内部备注，不记录敏感来源细节。
- **R119**：重构数据目录：现货价格迁移至 `data/spot_price/{region}/`，统调负荷迁移至 `data/dispatch_load/{region}/`，中长期/日前持仓迁移至 `data/trading_strategy/{contract_position|dayahead_position}/{profile}/`。
- **R120**：术语统一：后台和前端用户可见文案统一称“统调负荷”，不再使用“系统负荷”指代中长期分解曲线数据。
- **R121**：清理不采纳业务口径：从当前计算模型、批发结算配置和 UI 选项中移除阻塞成本字段与节点电价抽象；读取层兼容旧阻塞列但丢弃，不参与计算。
- **R122**：新增需量/容量单价后端数据文件 `data/demand_capacity/{region}/standard.csv` 和 `ConfigLoader.load_demand_capacity()` 读取入口，前端入口后续确定。
- **R123**：补充审计测试与修正旧测试预期，覆盖 region 数据定位、交易策略 profile 定位、光伏接入后的负荷守恒、API 契约和数据可信度基础文件。

---

## 第 13 轮（删除旧前端实现，2026-06-07）

- **R124**：删除已废弃的 Streamlit demo 前端（`app.py`、`src/ui/`、`scripts/run_streamlit.*`、`.streamlit/`），项目唯一可运行前端入口为 `frontend/` SPA，经 `run.py` 启动的 FastAPI 服务在 `http://127.0.0.1:8000/` 静态托管。
- **R125**：Python 依赖清单移除旧 Streamlit/Plotly/Matplotlib/Seaborn UI 依赖，保留核心计算、数据处理、FastAPI 后端和 pytest 测试依赖。

---

## Backlog 与文档索引

- **已入 PRD 的条目**：以 [PRD_v1.0.3.md](PRD_v1.0.3.md) 为准，并在本文件对应 Rxxx 处标注「已入PRD」。
- **讨论中、尚未写入 PRD 正文**：继续以「第 N 轮」追加；合并入 PRD 时同步更新 [CHANGELOG.md](CHANGELOG.md) 与（如适用）[implementation_matrix.md](implementation_matrix.md)。
- **版本与协作约定**：[VERSIONING.md](VERSIONING.md)；**交互纪要**：[SESSION_LOG.md](SESSION_LOG.md)；**实现登记**：[IMPLEMENTATION_LOG.md](IMPLEMENTATION_LOG.md)。

---

## 第 14 轮（夜间多页面工作台与帮助系统，2026-06-08）

- **R126**：单方案分析槽位扩展为 A/B/C/D，底部“比较”动作应进入真实“多方案对比”页面，并自动带入已记录槽位，不再停留在占位弹窗。
- **R127**：新增“多方案对比”工作台，最多支持 4 个方案横向对比；对比对象应允许分别指定方案、电价模式和商业模式，并展示社会福利、用户节费、储能 IRR、回收期、套利收益、循环次数、售电利润、光伏 IRR、光伏发电量、光伏消纳率、负荷变异系数等关键指标。
- **R128**：后端新增 `POST /api/compare`，作为多方案对比页的稳定契约；超出 4 个方案时应返回明确错误。
- **R129**：新增“方案管理”页面和 CRUD API，支持历史方案列表、新建、改名、复制、加载、删除；当前阶段优先保证管理闭环，完整参数快照语义后续继续细化。
- **R130**：新增“模型管理”页面和模型目录 API，按调度模型、零售电价模型、商业模式/优化目标、批发侧结算模型、投资评价模型分类展示当前系统模型。
- **R131**：模型管理页允许对已成型算法保存超参草稿；当前先落盘至 `data/model_params/{model_id}.json`，后续再逐项接入真实计算引擎。
- **R132**：缺省参数页面新增“数据资产总览”，扫描并展示 `data/` 下全局参数、分时电价、现货价格、统调负荷、用户典型负荷、光伏出力、交易策略、需量/容量单价、数据可信度等资产。
- **R133**：新增帮助文档系统，左侧提供文档列表和搜索，右侧展示正文；文档源包括内置帮助、`docs/*.md` 和 `PRD/*.md`。
- **R134**：后端新增文档 API：`GET /api/docs` 和 `GET /api/docs/{doc_id}`，并限制只能读取项目根目录下的 Markdown 文档。
- **R135**：`ScenarioConfig.ess_params`、`financial_params` 和 `private_overrides` 必须进入运行时计算；解析优先级为 `private_overrides > 方案私有参数 > 全局默认参数`。API 概览展示的储能参数也必须与该解析结果一致。
- **R136**：新增工作台 API 契约测试和方案 CRUD 测试；方案私有储能功率覆盖调度上限由已知失败项转为必须通过项。
- **R137**：`/api/calculate` 的缓存 key 必须包含方案内容指纹和全局参数文件版本，避免用户调整参数后拿到旧计算结果。
- **R138**：模型超参草稿保存后必须可在 `GET /api/models` 中回显，并在前端模型卡片显示草稿状态；模型 ID 写入路径必须做安全校验。
- **R139**：删除旧的非 `.v4` 前端 JS/CSS 残留文件；当前有效前端资源统一使用 `.v4` 文件，并在 `index.html` 中追加版本参数降低浏览器缓存风险。
- **R140**：新增产品复盘与路线文档 `docs/PRODUCT_REVIEW_AND_ROADMAP_20260608.md`，明确项目定位为面向中国电力市场的源网荷储售一体化策略仿真与决策系统，并将方案快照契约列为下一阶段最高优先级。

## 第 15 轮（光伏典型日单位出力曲线数据资产，2026-06-09）

- **R141**：新增光伏典型日单位出力曲线数据资产，基于清洗后的泛能光伏站点 15 分钟数据，按“省份-城市-季节-天气/典型类型”四维生成 `data/pv_typical_curves/`，每类仅保留一条 1MW 装机单位出力曲线。
- **R142**：新增 `data/pv_typical_curves/catalog.csv` 与 `summary.json`，记录曲线路径、单位、样本天数、来源站点数、代表日期和分类方法，供后续前端四级联动与后端曲线加载逻辑接入。
- **R143**：新增 `scripts/build_pv_typical_curves.py`，支持从桌面清洗数据和修复清单重新生成典型曲线；输出 CSV 固定为 1440 行分钟级数据，核心字段为 `output_kwh_per_mw`、`capacity_factor`、`power_kw_per_mw`。
- **R144**：光伏典型曲线省份目录统一使用英文行政区命名，并对河北电网分区做特殊约定：河北南网使用 `Hebei_south`，冀北使用 `Hebei_north`。

---

## 第 16 轮（现货典型月价格曲线数据资产，2026-06-09）

- **R145**：新增现货典型月价格曲线数据资产，基于用户提供的 `spot_fixed` 全部标准化源目录和项目内现货样例，按“省份-年份-月份”生成 `data/spot_typical_prices/`，每条曲线固定包含 24 小时日前与实时成对价格。
- **R146**：新增 `data/spot_typical_prices/catalog.csv`、`audit.csv` 与 `summary.json`，记录曲线路径、单位、来源类型、完整日数量、可信等级、异常剔除行数与拒绝原因，供后续前端选择和后端加载接入。
- **R147**：新增 `scripts/build_spot_typical_prices.py`，支持从逐日小时明细、15 分钟样例、月均样例和项目既有现货数据重新生成典型月均曲线；缺失日前/实时任一侧、小时不完整或超出价格边界的数据不得进入最终曲线。

---

## 第 17 轮（方案快照与 A/B/C/D 契约加固，2026-06-09）

- **R148**：将 `wholesale_overrides` 纳入方案与子方案快照正式字段，保证批发侧规则、合约 profile 和日前 profile 可随 A/B/C/D 保存、加载、计算和对比复现。
- **R149**：`/api/calculate` 与 `/api/compare` 必须基于同一份子方案快照解析运行配置，同一快照下核心指标应保持一致，缓存 key 应纳入解析后的批发侧配置与方案指纹。
- **R150**：前端单方案页由 `workbench.v4.js` 统一管理 A/B/C/D 子方案状态，点击子方案应先暂存当前 UI 到内存，再加载目标快照并重新计算，不得由旧槽位逻辑覆盖按钮状态。
- **R151**：方案管理页加载指定子方案时应直接激活目标 A/B/C/D 并计算，不得先计算 A 后再切换导致结果与当前激活子方案错位。
- **R152**：商业模式分组作为本轮 UI 快照字段保存和复现，但暂不映射为新的后端商业模型；未完成设计的电价曲线选择不进入本轮正式计算契约。

---

## 第 18 轮（单日典型曲线模拟契约，2026-06-09）

- **R153**：单日简单模拟仅允许选择后台典型曲线，不允许在前端上传、自定义或手动编辑曲线。
- **R154**：新增单日典型曲线 catalog 服务和 `GET /api/simple-day/catalog`，统一枚举负荷、光伏、现货、零售电价和批发 profile 选项。
- **R155**：方案与 A/B/C/D 子方案快照必须保存曲线 ID/profile ID，并在加载、计算、保存、刷新和对比时按同一 ID 复现。
- **R156**：单日简单模拟结果需标注“单日典型曲线 | 月度*30 | 年度*365”，月度和年度指标仅作为典型日粗略折算。
- **R157**：多日深度模拟、用户导入自定义曲线和数据管理模块暂不在本轮实现。
- **R158**：单日简单模拟和未来多日深度模拟共用光伏、储能、财务、商业模式、调度算法、结算模型和投资评价模型，差异仅在曲线数据层。

*下一轮需求讨论后继续追加...*
